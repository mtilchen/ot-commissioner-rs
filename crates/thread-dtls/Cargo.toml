[package]
name = "thread-dtls"
description = "DTLS 1.2 commissioning profile for Thread MeshCoP: EC J-PAKE key exchange, TLS 1.2 PRF key schedule, and AES-128-CCM-8 record protection"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
default = []
# In-process loopback DTLS server for deterministic handshake tests. Unstable
# test scaffolding for this workspace's suites; not a supported public API.
test-support = []

[dependencies]
aes.workspace = true
ccm.workspace = true
hex.workspace = true
hmac.workspace = true
p256.workspace = true
rand_core.workspace = true
sha2.workspace = true
subtle.workspace = true
thiserror.workspace = true
tokio.workspace = true
zeroize.workspace = true
